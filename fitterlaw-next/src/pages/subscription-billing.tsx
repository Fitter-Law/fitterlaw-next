import DefaultLayout from '@/components/Layouts/DefaultLayout';

export default function subscription_billingPage() {
 return (
  <DefaultLayout>
    <div dangerouslysetinnerhtml="{{" __html:="" `<!--="" wp:pmpro="" billing-page="" --="">` }} /&gt;
    </div>
  </DefaultLayout>
);
}