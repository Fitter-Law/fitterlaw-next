import DefaultLayout from '@/components/Layouts/DefaultLayout';

export default function new_client_applicationPage() {
 return (
  <DefaultLayout>
    <div dangerouslySetInnerHTML={{ __html: `[fluentform="" id="12" ]` }}="">
    </div>
  </DefaultLayout>
);
}