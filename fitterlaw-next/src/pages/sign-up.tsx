import DefaultLayout from '@/components/Layouts/DefaultLayout';

export default function sign_upPage() {
 return (
  <DefaultLayout>
    <div dangerouslySetInnerHTML={{ __html: `[fluentform="" id="46" ][pmpro_login="" show_menu="0" show_logout_link="true" ]` }}="">
    </div>
  </DefaultLayout>
);
}